# Load local, untracked configuration when it is available.
spring.config.import=optional:file:./backend/.env[.properties]
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=${MONGODB_DATABASE}